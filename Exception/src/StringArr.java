import java.lang.reflect.Array;
import java.util.Scanner;

public class StringArr {
    }/* throws MyArraySizeException {
        Scanner sc = new Scanner (System.in);
        System.out.println ("введите число строк массива");
        int i = sc.nextInt ();
        System.out.println ("введите число столбцов массива");
        int j = sc.nextInt ();
        this.arr = new String[i][j];
        if (arr.length!=4){ throw new MyArraySizeException("Число строк массива меньше 4х");}
        for (int a = 0; a<arr.length; a++) {
            if (arr[a].length != 4) {throw new MyArraySizeException ( "Число столбцов массива меньше 4х" ); }
        }
    }*/
/*    public void arr (String arr[][]) throws MyArraySizeException {
        if (arr.length!=4){ throw new MyArraySizeException("Число строк массива меньше 4х");}
        for (int i = 0; i<arr.length; i++) {
            if (arr[i].length != 4) {throw new MyArraySizeException ( "Число столбцов массива меньше 4х" ); }
        }
    }*/

